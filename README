Installation (pull-only repo)

    git clone git://github.com/jaredcobb/dotvim.git ~/.vim

Installation (ssh)

    git clone git@github.com:jaredcobb/dotvim.git ~/.vim

Symlinks

    ln -s ~/.vim/vimrc ~/.vimrc
